// template class 
// 컴파일 시점에 instantiation됨
// class 이름<T>, T는 이름붙일수 있는 것들

// function도 가능
class A<F>
{
    void Main()
    {
        F(3, 4, 5);
    }
}

// type 지정해주기
// 주의 해야할 것은 O는 Object의 인스턴스가 아니라 Object를 상속받는 클래스
class A<Object O, B>
{
    
}
