
// namespace 용법

namespace A
{
    void FuncA();    // 선언만
}

namespace A
{
    void FuncA() { } // 중복해서 정의할 수 있고, 
}

// nested namespace
namespace A.One
{
    // nested라고 해서 A에 있는 함수를 사용할 수는 없다? --> 있어야 한다

    // 왜?
    // global namespace와 consistency를 맞춰야 되서..
    //  - 최상단은 접근 가능하면서 부모 namespace는 접근할 수 없다는게 이상함

    // global namespace를 그냥 접근 못하게 할까?
    //  - 불편함
}

// external

namespace B
{
    void FuncB();
}

// 최상위 레퍼런스
void G();

// FuncA 를 현재 스코프로 가져옴
// 정의가 따라오는 것이 아닌 사용할때 위치를 룩업하는 부분을 가져오는 것이다
// using [namespaceName]
// using [namespaceName].[namedObject]

using A.FuncA; // 이 위치에서 FuncA가 있는지 찾아본다
using B;       //



