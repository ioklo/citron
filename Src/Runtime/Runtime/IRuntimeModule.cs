using Citron.CompileTime;
using System;
using System.Collections.Generic;
using System.Text;

namespace Citron.Runtime
{
    //public interface IRuntimeModule : IModule
    //{
    //    Value MakeBool(bool b);
    //    Value MakeInt(int i);        
    //    // Value MakeEnumerable(QsDomainService domainService, QsTypeValue elemTypeValue, IAsyncEnumerable<Value> asyncEnumerable);
    //    // Value MakeList(QsDomainService domainService, QsTypeValue elemTypeValue, List<Value> elems);
    //    ObjectValue MakeNullObject();

    //    int GetInt(Value value);
    //    void SetInt(Value value, int i);

    //    bool GetBool(Value value);
    //    void SetBool(Value value, bool b);

    //    string GetString(Value value);
    //    void SetString(DomainService domainService, Value value, string s);

    //    void SetList(DomainService domainService, Value value, TypeValue elemTypeValue, List<Value> elems);
    //    void SetEnumerable(DomainService domainService, Value value, TypeValue elemTypeValue, IAsyncEnumerable<Value> asyncEnumerable);
    //}
}
