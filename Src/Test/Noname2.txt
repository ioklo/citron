
AssignExp = 
    LValueExp = Exp

LValueExp = 
    Var 
    LValueExp . "id"    // record lookup
    LValueExp [ "id" ]  // array lookup
    LValueexp() // call object returned call

// 이건 되어야 합니까
1.ReturnValueTypeFunc().a = 1;
1.ReturnRefTypeFunc().a = 1;

// 

x . y => bin


. binop (object . identifier) => loc or value
x [y] binop (object . 



x.y.z.w = > ((x.y).z).w



// Type 따라 달라집니다. => Type 체커에서 걸러내도록

a + b = 3; a + b가 lvalue가 될 수 있는가

pushLocal a <- 
pushLocal b
plus
----- 값이 있을 것임
pushValue 3
assign 

a.b = 3;

pushLocal a
pushRecord "b" // a.b를 

a.b.c.ToString().p = b = b.AddV + 3;

// b = b.+ 3;
// a = (계산된 결과)

pushLocal c
pushLocal 3
add

// assign 계산 순서;;
pushLoc b

Assign (loc), (val) = (val)

pushLoc a


// 둘을 구분할 재간이 없네요

f <- lvalue? lvalue



// LValueExp 

a[2].asdfiasj.asdfiasjdfa.sadfij();



