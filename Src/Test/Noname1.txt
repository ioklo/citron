int type의 위치,


Primitive Type(int, string, bool, void, Object)는 누가 갖고 있어야 할까요?

1. IL       => 이건 구조인데 자꾸 인스턴스를 삽입하려고 하면 안되지 않을까요
2. Compiler =>
3. 또 다른 무언가


Environment는 인스턴스들을 넣는 공간인데..

그럼 IL에는 없어야 하지 않을까..

1. IL에 인스턴스도 같이 삽입 시키고 나중에 분리한다
2. 지금 분리해서 작동시킨다.

IType
 - RefType (Object, Ref<int>, ...)
 - ValueType
 - EnumType
 - FuncType
 - VoidType

1. ValueType : IType

2. RefType : IType

1. IntType : ValueType 

2. StringType : RefType

3. BoolType : ValueType

4. VoidType 

5. ObjectType : RefType 

or

instance로 그냥 들고 있게 할 수 도 있다.

ValueType IntType;
RefType StringType;
ValueType BoolType;
VoidType VoidType;


IValue (instances)
 - RefTypeValue (Object)
 - 

// 이런 것들이 IL안에 들어가 있으면 안될 것 같다.

IValue value (instances)



